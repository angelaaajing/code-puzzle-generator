export const GRID_CONFIG = {
  // Vertical spacing
  ROW_HEIGHT: 3, // rem units
  
  // Horizontal spacing (indentation)
  INDENT_SIZE: 2, // rem units (equivalent to 4 spaces in monospace font)
  
  // Number of visible rows in the canvas
  ROWS: 20,
  
  // Snapping threshold in pixels
  SNAP_THRESHOLD: 8,
}; 